# Copy this file to .env if the API is not on the same origin.
# Local Vite already proxies /api to http://localhost:5000
# Leave empty to use /api, or set a full URL for production.

VITE_API_BASE_URL=
